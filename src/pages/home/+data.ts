import { PageContextServer } from 'vike/types';

export { data };
async function data(pageContext: PageContextServer){
//   const userData = await fetcher('users/self');
//   console.log(userData);
//   return { userData };
}